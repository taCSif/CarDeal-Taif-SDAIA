name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install -e '.[dev]'
      - run: ruff check src tests scripts
      - run: mypy src
      - run: lint-imports
      - name: Secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: python -m pytest -m 'not real_model'

  docker:
    runs-on: ubuntu-latest
    needs: quality
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install -e '.[dev]'
      - name: Compute image tag
        run: echo "IMAGE=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]'):${{ github.sha }}" >> "$GITHUB_ENV"
      - name: Train release model
        run: python scripts/train.py
      - name: Run real-model behavior and golden tests
        run: python -m pytest -m real_model --no-cov
      - name: Build image
        run: docker build -t "$IMAGE" .
      - name: Start container
        run: docker run -d --name app -p 8000:8000 "$IMAGE"
      - name: Readiness smoke test
        run: |
          for i in $(seq 1 30); do curl -fsS http://localhost:8000/ready && exit 0 || sleep 2; done
          docker logs app
          exit 1
      - run: python scripts/smoke.py
      - name: Record image size
        run: docker image inspect "$IMAGE" --format '{{.Size}}' > image-size-bytes.txt
      - name: Verify image is <= 500 MB
        run: test "$(cat image-size-bytes.txt)" -le 524288000

  publish:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: docker
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install -e '.[dev]'
      - name: Compute image tag
        run: echo "IMAGE=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]'):${{ github.sha }}" >> "$GITHUB_ENV"
      - run: python scripts/train.py
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - run: docker build -t "$IMAGE" .
      - run: docker push "$IMAGE"
